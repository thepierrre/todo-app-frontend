import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#576CBC",
    },
    light: {
      main: "rgb(241, 241, 241)",
    },
    grey: {
      main: "rgb(135, 135, 135)",
    },
  },
});

export default theme;
